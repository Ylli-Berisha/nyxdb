---
name: feedback-commits
description: User always handles commits themselves — never commit on their behalf
metadata:
  type: feedback
---

Never run `git commit` or `git push`. The user always commits and pushes themselves.

**Why:** User explicitly said "I'll always commit."
**How to apply:** Stage files if needed, show what's ready, but stop there. Never commit.
